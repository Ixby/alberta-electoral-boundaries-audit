// Translation in progress — partial file removed because it was truncated
// mid-write by a session limit. Every key falls back to English via
// dict.ts until the next pass.
export default {} as const;
